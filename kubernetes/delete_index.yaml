apiVersion: batch/v1
kind: Job
metadata:
  name: es-delete-index
spec:
  template:
    spec:
      containers:
      - name: delete-index
        image: vluciani/node-search-app:python-indexation-1.12
        command: ["python", "elastic_setup/delete_index.py"]
        env:
        - name: ELASTICSEARCH_URL
          value: "http://elasticsearch:9200"
        - name: ELASTIC_USER
          value: "elastic" 
        - name: ELASTIC_PASSWORD
          valueFrom:
            secretKeyRef:
              name: elastic-secret 
              key: password
      restartPolicy: Never